git add .
  Add unsaved files to git.  This adds to your local git but not to github.  You'll need additional commands to send to github

git branch

git branch -D

git checkout <branch>

git checkout -b <branch>

git clone url

git commit -m"FILE DESCRIPTION"

git fetch

git init

git merge <branch>

git pull

git push

git remote add origin (url)

git stash

git status
  Give information about git, including your branch and files that have not been 'saved.'
